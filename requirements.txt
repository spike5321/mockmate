# MockMate 依赖清单
# 安装：pip install -r requirements.txt

# --- 模型调用 ---
# 用 requests 直接打智谱的 OpenAI 兼容接口，不引入官方 zhipuai SDK。
# 原因：zhipuai 有一个没写进依赖清单的隐形依赖 sniffio，装完直接 import 崩。
requests>=2.31.0
python-dotenv>=1.0.0

# --- 知识库检索层（kb/）---
# 向量库。持久化到 .kb/ 目录，可用 `python -m kb.build` 重建。
chromadb>=0.5.0
# 解析 PDF 语料（knowledge/ 里目前是 .md，需要放 PDF 时才用到）
pypdf>=4.0.0
# 本地向量模型（免 Key 的兜底路径）。
# 没配 ZHIPU_API_KEY 时 kb/ 会自动改用它，评审 clone 下来不填任何配置
# 也能建库、能检索。首次运行会下载约 91MB 的中文向量模型。
# 跑在 onnxruntime 上，不依赖 torch。
fastembed>=0.4.0

# --- 测试 ---
pytest>=8.0
