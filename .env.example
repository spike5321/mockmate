# 复制这个文件为 .env，然后填入你自己的 Key
# 注意：.env 已在 .gitignore 里，永远不会被提交

# ── 用哪家模型 ─────────────────────────────────────────────────────────
# 大多数情况不用管这里：模型名自己就说明了它属于哪家（deepseek-chat → DeepSeek）。
# 想固定下来才设这一行。可选：zhipu / deepseek / ollama
# LLM_PROVIDER=zhipu

# 想接任何别的 OpenAI 兼容端点（自建网关、别的云），设这三个就够，**不用改代码**：
# LLM_BASE_URL=https://your-gateway.example.com/v1
# LLM_API_KEY=
# LLM_MODEL=

# 智谱开放平台 API Key（https://open.bigmodel.cn/  →  API Keys）
ZHIPU_API_KEY=

# 其他供应商的 Key —— 用到哪家就填哪个（只读，不会被打进日志或提交）
# DEEPSEEK_API_KEY=
# OLLAMA_API_KEY=        # 本地模型本来就不需要 Key，留空即可

# 面试官（Agent 主循环）用的对话模型
#   glm-4.5-flash —— 免费，支持 function calling，本项目默认用它
#   想换更聪明的模型，改这一行就行（代码不用动）
CHAT_MODEL=glm-4.5-flash

# 评分模型（可选，不填 = 和 CHAT_MODEL 用同一个）
#
# 单独配置评分模型有两个好处：
#   1. 错开限流 —— 智谱的 429 是按「模型」报的（原文：「**该模型**当前访问量过大」），
#      面试官和评分官各占一个模型的额度，不容易一起被打满。
#      评分调用会让整场面试的请求数翻一倍左右，撞限流的概率明显上升。
#   2. 出题的和判卷的换成不同模型，评分视角更独立。
#
# SCORE_MODEL=glm-4-flash

# 备选模型（可选）。配了它，面试中途模型罢工时系统会**自动**换过去接着跑 ——
# 这是失败升级阶梯的第二级，不用你守着。
#   建议填**同一家的**另一个模型（比如 glm-4.5-air）：
#   模型名跨供应商是不通用的，写别家的名字在那边会直接报"模型不存在"。
# FALLBACK_MODEL=glm-4.5-air

# --- 向量化（知识库检索用）---
# 走哪家算向量：
#   auto  —— 默认。有 ZHIPU_API_KEY 就用智谱 embedding-3（2048 维），
#            没有就自动用本地模型（512 维，免 Key 免网络）。
#   zhipu —— 强制智谱。没有 Key 直接报错，不会静默降级。
#   local —— 强制本地模型。
# EMBEDDING_PROVIDER=auto

# 本地向量模型名（默认 BAAI/bge-small-zh-v1.5）。想换模型改这里。
# ⚠ 换 embedding 模型（含 local 与 zhipu 之间切换）**必须重建向量库**：
#     python -m kb.build --rebuild
#   不是因为维度可能对不上（那种 Chroma 会直接报错，你看得见），
#   而是不同模型的向量空间不可比 —— 混在一个库里检索不会报错，
#   只是结果失去意义。
# LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 本地模型缓存目录，默认 ~/.cache/fastembed（首次运行自动下载约 91MB）。
# FASTEMBED_CACHE_PATH=
