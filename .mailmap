# 统一作者署名，让 `git log` / `git shortlog` 把历史提交归到同一个身份。
#
# ★ 重要：GitHub 网页的贡献图**不读**这个文件 —— 它只认「提交邮箱是否关联到账号」。
#   所以除了这里，还要去 GitHub → Settings → Emails 把下面的历史邮箱加进账号。
#   本仓库那个历史邮箱是 1450854842@qq.com（能收验证信，加进去即可生效）。
#
# 格式：正确姓名 <正确邮箱> <历史邮箱>
Spike <222852730+spike5321@users.noreply.github.com> <1450854842@qq.com>
