# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# 临时文件
*.log
*.tmp
style_preview/

# 密钥与本地配置（绝对不能提交）
.env
.env.local

# 每次运行都会重新生成的报告产物（保持工作区干净）
scenarios/*.report.json
scenarios/*.report.md

# Agent 运行轨迹
traces/

# 运行产物：调试日志、checkpoint 等（本地留档，不入库）
# 跑面试时建议重定向到这里：python -u orchestrator.py --scenario backend_intern > runs/run.log
runs/

# 知识库向量索引（二进制产物，可用 python -m kb.build 随时重建，不入库）
.kb/
.test-tmp-*/
.pytest_cache/
